// Local PostCSS config that overrides any parent/workspace config.
// Tailwind CSS v4 is handled by the @tailwindcss/vite plugin, so no
// PostCSS plugins are required here.
export default {
  plugins: {},
};
